;;; config/YASnippet

;; [2017-09-27] not use yet

(use-package yasnippet
  :disabled
  :hook (prog-mode . yas-minor-mode)
  :config nil)

;; (when (fboundp 'yas-global-mode)
;;   (yas-global-mode -1)
;;   (warn "誰が YASnippet を有効にしている？"))
